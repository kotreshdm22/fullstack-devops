version: '3.9'

services:
  backend:
    build:
      context: ./backend
    container_name: spring-backend
    ports:
      - '8090:8090'

  frontend:
    build:
      context: ./frontend
    container_name: react-frontend
    ports:
      - '3000:80'
    depends_on:
      - backend
